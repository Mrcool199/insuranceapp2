<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Platinum"
        android:supportsRtl="true"
        android:theme="@style/Theme.InsuranceApp"
        tools:targetApi="31" >
        <activity
            android:exported="true"
            android:name="LoginActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        <activity
            android:name="SignupActivity"
            android:exported="true" />
        <activity
            android:name="Success"
            android:exported="true" />
        <activity
            android:name="AddAsset"
            android:exported="true" />
        <activity
            android:name="ProfileActivity"
            android:label="ProfileActivity" />
        <activity
            android:name="ClaimsActivity"
            android:label="ClaimsActivity" />
        <activity
            android:name="ClaimsActivity2"
            android:label="ClaimsActivity" />
        <activity
            android:name="ClaimsActivity3"
            android:label="ClaimsActivity" />
        <activity
            android:name="ClaimsActivity4"
            android:label="ClaimsActivity" />

    </application>

</manifest>
